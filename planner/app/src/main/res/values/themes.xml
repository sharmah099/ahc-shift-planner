<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--<style name="Theme.Nomad" parent="@android:style/Theme.Holo.Light">-->
    <style name="Theme.Nomad" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textViewStyle">@style/Text</item>
        <item name="android:buttonStyle">@style/Button</item>
        <!--<item name="android:radioButtonStyle">@style/RadioButton</item>-->
        <item name="colorPrimary">@color/action_bar_grey</item>
        <item name="colorPrimaryDark">@color/status_bar_grey</item>
        <item name="colorAccent">@color/primary_blue</item>

        <!-- remove this line (below) to go back to the default (electric blue) selector color -->
        <!-- Below line is commented to apply default ripple effect for 5.0 and above-->
        <!--<item name="android:selectableItemBackground">@drawable/selectable_back</item>-->
        <!-- styles of attributes defined in the framework library -->
        <item name="buttonStyle">@style/Button</item>
    </style>

    <style name="Theme.Nomad.NoTitleNoActionBar" parent="Theme.Nomad">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

</resources>